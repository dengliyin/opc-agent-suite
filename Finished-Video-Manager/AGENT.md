# 成品管理

这是一个独立的本地 Web agent。

目标：

1. 扫描 Obsidian 成品视频目录。
2. 按产品、国家和生成信息展示成品视频。
3. 读取视频标题库 Markdown，展示对应产品和国家的标题标签池。
4. 页面风格参考 `Video-Collection`，但不依赖它的运行配置。

默认端口：`9996`。

标题库约定：

- 国家标题格式：`## 国家：泰国 (TH)`
- 内容表格格式：`### 标题标签池`
- 每行是一条标题，标题后面直接接 5 个标签。
- 成品视频文件名里如果出现多个国家码，只取第一个国家码。

# AI＋实拍混剪智能体

- 端口：`10000`
- 独立目录、独立 Python 虚拟环境。
- 输入只读取 `05AI片段`、`06产品介绍音频`、`07实拍素材`。
- 中间产物和使用历史只写入 `08混剪工作区`。
- 成片统一写入共享的 `成品视频/<产品名>/`。
- 不改写钩子、CTA 或产品介绍音频的内容。
- 使用 HyperFrames 完成最终渲染，FFmpeg 负责媒体探测、裁切和标准化。

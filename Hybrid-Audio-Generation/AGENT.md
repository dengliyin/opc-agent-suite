# 配音智能体

- 端口：`10004`
- 独立目录、独立 Python 虚拟环境。
- 只读取 `AI实拍混剪/06音频文案/*.md`。
- 只识别同时包含“建议音频文件名”和“音频文案”的三级标题段落。
- 只把“音频文案”原语言内容送入配音，不读取“中文校对”。
- 使用 HyperFrames 本地 Kokoro TTS，固定以 `1.2` 倍自然语速生成。
- 输出为 AAC 编码的 M4A，保存到 `AI实拍混剪/06音频文件/<产品名>/`。
- 默认不覆盖同名音频；只有用户明确勾选覆盖才会重新生成。
